# Include GNU coreutils aliases if installed via homebrew

ALIASES=$(brew --prefix coreutils)/aliases
if [ -e $ALIASES ]; then
    source $ALIASES
    # Whilst experimenting I had probes with my prompt and g[
    unalias [
fi
